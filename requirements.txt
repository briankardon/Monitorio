# Monitorio Python calibration tool dependencies.
# Target: Python 3.12+ (developed against 3.14).

# Display / drawing. pygame-ce is preferred over pygame proper because
# it publishes wheels for new Python versions faster.
pygame-ce>=2.5

numpy>=1.26

# NI-DAQmx Python wrapper. Requires the NI-DAQmx driver to be installed
# separately on the host OS (not pip-installable).
nidaqmx>=1.0

matplotlib>=3.8
